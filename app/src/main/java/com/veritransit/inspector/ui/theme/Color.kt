package com.veritransit.inspector.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * VeriTransit design tokens — "Field Operational Precision".
 * Warm alabaster canvas, deep transit burgundy, restrained diagnostic accents.
 */
object VT {
    // Canvas & surfaces
    val Alabaster = Color(0xFFF4F3F0)
    val Surface = Color(0xFFFFFFFF)
    val Inset = Color(0xFFEFEDE9)
    val Hairline = Color(0xFFE2E8F0)

    // Brand
    val Primary = Color(0xFF7E1530)
    val PrimaryDeep = Color(0xFF5C001D)
    val PrimaryPressed = Color(0xFF4C0C1C)
    val OnPrimary = Color(0xFFFFFFFF)

    // Text
    val Ink = Color(0xFF0F172A)
    val Slate = Color(0xFF334155)
    val Muted = Color(0xFF64748B)
    val Faint = Color(0xFF94A3B8)
    val Border = Color(0xFFCBD5E1)

    // Diagnostic accents
    val Emerald = Color(0xFF059669)
    val EmeraldBg = Color(0xFFECFDF5)
    val EmeraldLine = Color(0xFFA7F3D0)
    val Amber = Color(0xFFB45309)
    val AmberDot = Color(0xFFD97706)
    val AmberBg = Color(0xFFFFFBEB)
    val AmberLine = Color(0xFFFDE68A)
    val Crimson = Color(0xFFB91C1C)
    val CrimsonBg = Color(0xFFFEF2F2)
    val CrimsonLine = Color(0xFFFECACA)
    val Azure = Color(0xFF0284C7)
    val AzureBg = Color(0xFFF0F9FF)
}
